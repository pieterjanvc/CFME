% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_helper.R
\name{db_fetch_review_reanchor}
\alias{db_fetch_review_reanchor}
\title{Fetch review info and unplaced competency evidence for the re-anchor step}
\usage{
db_fetch_review_reanchor(conn, review_ids, force = FALSE)
}
\arguments{
\item{conn}{DB connection}

\item{review_ids}{Integer vector of review_assignment IDs}

\item{force}{Skip the statusCode filter. Default = FALSE}
}
\value{
Data frame with one row per review: \code{review_id}, \code{rubric_id},
\code{evaluation} (character), and a list-column \code{items} (data frame:
\code{itemId}, \code{competency_text_id}, \code{competency_name}, \code{text}). Reviews with
no unplaced rows are dropped. NULL if there is nothing to process.
}
\description{
For each review, collects every \code{competency_text} row that has no located
position (\verb{start IS NULL}) - the paraphrased / unlocatable AI quotes - plus
the evaluation text rendered the same way the extraction step saw it
(\code{dbGetEvals(html = FALSE)} then \code{dedupe_repeated_paragraphs()}). Filters to
\code{statusCode == 5} (Batch scoring complete) unless \code{force = TRUE}.
}
\details{
Rows already flagged \code{locate_status = 'unlocated'} are still returned so a
later run can retry them; \code{dbCompReanchorApply()} clears or re-sets the
flag based on the new answer.
}
