% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbOperations.R
\name{reanchor_locate}
\alias{reanchor_locate}
\title{Internal: locate a re-anchor span, tolerating a differing leading subject}
\usage{
reanchor_locate(plainText, anchor)
}
\arguments{
\item{plainText}{Tag-stripped evaluation text (\code{db_locate_text()})}

\item{anchor}{The model's returned span}
}
\value{
Length-2 integer vector \code{c(start, end)} (0-indexed, half-open) or
\code{NULL} if the span (or its subject-stripped remainder) isn't present.
}
\description{
The re-anchor model reliably copies the body of a span but often keeps the
paraphrase's subject word instead of the evaluation's - \code{She} for \code{she},
\verb{[name_redact]} for \code{He}, and vice versa. When an exact (whitespace-
tolerant) \code{mod_highlight_locate()} fails, this strips the anchor's first
token, re-locates the remainder, and walks the match start left over
whatever subject token actually precedes it in \code{plainText}.
}
