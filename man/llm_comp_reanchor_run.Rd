% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review.R
\name{llm_comp_reanchor_run}
\alias{llm_comp_reanchor_run}
\title{Re-anchor paraphrased competency evidence for a set of reviews (real-time)}
\usage{
llm_comp_reanchor_run(
  conn,
  review_ids,
  model = "gpt-5.1",
  endpoint = "https://azure-ai.hms.edu",
  verbose = FALSE,
  force = FALSE
)
}
\arguments{
\item{conn}{DB connection}

\item{review_ids}{Integer vector of review_assignment IDs to process}

\item{model}{Azure deployment name. Default = "gpt-5.1"}

\item{endpoint}{Azure endpoint base URL}

\item{verbose}{Print progress messages. Default = FALSE}

\item{force}{Process even if not at statusCode 5. Default = FALSE}
}
\value{
Data frame summarising results per review (review_id, statusCode,
n_items, n_reanchored, n_flagged, call_status, tokens_in, tokens_out), or
NULL if none of the given IDs have anything to re-anchor
}
\description{
Phase 2 of the paraphrased-quote fix (dev/paraphrase_fix_plan.md). For each
review at statusCode 5 (Batch scoring complete) that still has
\code{competency_text} rows with no located position, builds one re-anchor
request (build_reanchor_items()) from the evaluation text and the unplaced
quotes, asks the LLM for the verbatim span each quote refers to
(llm_comp_reanchor()), and applies the answer via dbCompReanchorApply().
}
\details{
Per review, the resulting statusCode is:
\itemize{
\item 3  if any row was re-anchored (text_match changed) and no rule-2
conflict resulted - ready for re-scoring
\item 6  if a re-anchor introduced a rule-2 conflict - needs resolve first
\item 5  unchanged (nothing re-anchored, or the API call failed) - any rows
the model couldn't anchor are now marked locate_status = 'unlocated'
for human review
}

No batch equivalent yet - see llm_comp_reanchor_batch_submit() when added.
}
