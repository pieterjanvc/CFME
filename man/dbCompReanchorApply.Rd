% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbOperations.R
\name{dbCompReanchorApply}
\alias{dbCompReanchorApply}
\title{Apply LLM re-anchor answers to a review's unplaced competency evidence}
\usage{
dbCompReanchorApply(conn, review_assignment_id, items, anchors, commit = TRUE)
}
\arguments{
\item{conn}{NARRATE database connection}

\item{review_assignment_id}{Review assignment ID}

\item{items}{Data frame mapping \code{itemId} -> \code{competency_text_id} (the
\code{items} list-column entry from \code{db_fetch_review_reanchor()})}

\item{anchors}{Parsed model answer (\code{llm_comp_reanchor()$data}): a list of
entries each with \code{itemId} and \code{anchor} (a string or \code{NULL})}

\item{commit}{(Default = TRUE)}
}
\value{
A list with \code{summary} (named integer: \code{n_reanchored},
\code{n_flagged}, \code{n_unanswered}) and \code{rows} (data frame: \code{competency_text_id},
\code{itemId}, \code{outcome}, \code{start}, \code{end}).
}
\description{
Phase 2 of the paraphrased-quote fix (dev/paraphrase_fix_plan.md). Given
the model's answer - one verbatim span, or \code{null}, per unplaced
\code{competency_text} row - this, per target row:
\itemize{
\item locates the returned span in the review's locate text
(\code{db_locate_text()}, whitespace-tolerant via \code{mod_highlight_locate()}).
The answered anchors are located together in \code{itemId} order so two
paraphrases of the same quote don't claim the same span; already-located
rows are \emph{not} seeded as prior claims and are never touched;
\item on a hit: rewrites \code{text_match} to the anchor, sets \code{start}/\code{end},
clears \code{locate_status};
\item on a miss (no answer, or the span isn't in the text): sets
\code{locate_status = 'unlocated'} and leaves the row unplaced for a human.
}
}
\details{
Overlap with an existing quote from another competency is \emph{not} prevented
here - that is a legitimate rule-2 conflict, and \code{llm_comp_reanchor_run()}
re-checks \code{dbCompExtractionCheckConflicts()} after applying and routes the
review to the resolve step (statusCode 6) when one results.
}
