% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbOperations.R
\name{dbRelocateCompText}
\alias{dbRelocateCompText}
\title{Backfill competency_text positions for AI evidence stored without one}
\usage{
dbRelocateCompText(
  conn,
  review_assignment_ids = NULL,
  apply_moves = FALSE,
  dry_run = FALSE,
  commit = TRUE
)
}
\arguments{
\item{conn}{NARRATE database connection}

\item{review_assignment_ids}{(Optional) Restrict to these review
assignments. Default: every AI (\code{reviewer_id} 1) review with at least one
\code{competency_text} row whose \code{start} is \code{NULL}.}

\item{apply_moves}{(Default = FALSE) Also write back already-located rows
whose recomputed position differs. When FALSE such rows are reported in
\code{moved} but left untouched.}

\item{dry_run}{(Default = FALSE) Compute and return the report without
writing anything.}

\item{commit}{(Default = TRUE) Commit the writes.}
}
\value{
A data frame, one row per processed review assignment:
\code{review_assignment_id}, \code{n_text} (rows considered), \code{filled}
(\code{NA} -> located), \code{moved} (located -> different span), \code{still_na}
(\code{NA} before and still unplaceable), \code{written} (rows actually updated).
}
\description{
\code{mod_highlight_locate()} records \code{start/end = NA} when a quote can't be
placed. Some of those are genuine paraphrases (the text is not in the
evaluation), but others are verbatim - or whitespace-equivalent - quotes
that were extracted before \code{mod_highlight_locate()} gained its
whitespace-tolerant fallback, so a position was simply never written. This
function recovers the second group. No LLM, no change to \code{text_match},
fully reversible from a database backup.
}
\details{
For each affected review the review's \strong{entire} \code{competency_text} set is
re-located in insertion (\code{id}) order, reproducing the non-overlapping
claim order \code{dbCompExtraction()} established, so a backfilled row claims a
span consistent with the rows around it. By default only rows that were
\code{NA} are written back; a row that already has a position is never moved
and never reset to \code{NA} (pass \code{apply_moves = TRUE} to also rewrite
already-located rows whose recomputed span differs - inspect the \code{moved}
count from a \code{dry_run} first).
}
