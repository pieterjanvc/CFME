% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbOperations.R
\name{db_locate_text}
\alias{db_locate_text}
\title{Rebuild the tag-stripped evaluation text competency_text offsets live in}
\usage{
db_locate_text(conn, review_assignment_id, redacted = NULL)
}
\arguments{
\item{conn}{NARRATE database connection}

\item{review_assignment_id}{Review assignment ID}

\item{redacted}{(Optional) The review_assignment's \code{redacted} flag, if
already loaded; looked up when missing. \code{NA} is treated as \code{TRUE}.}
}
\value{
A single tag-stripped character string.
}
\description{
\code{competency_text.start/end} are measured against the evaluation rendered
with \code{dbGetEvals(html = TRUE, subtitleTag = "b")} and then tag-stripped -
the exact string \code{mod_highlight_server} highlights against in
\code{inst/review_app.R} (\code{evalText()}). Both \code{dbCompExtraction()} (at
extraction time) and \code{dbRelocateCompText()} (for backfills) need that
string; this keeps the two in lockstep.
}
\keyword{internal}
